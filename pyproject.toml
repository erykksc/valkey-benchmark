[project]
name = "valkey-benchmark"
version = "0.1.0"
description = "The python project is used for analyzing the results created by running benchmark.go"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "jupyterlab>=4.5.3",
    "jupyterlab-vim>=4.1.4",
    "matplotlib>=3.10.8",
    "pandas>=3.0.0",
    "pyyaml>=6.0.3",
    "seaborn>=0.13.2",
]
